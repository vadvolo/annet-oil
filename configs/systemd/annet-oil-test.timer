[Unit]
Description=Schedule the annet-oil device health sweep

[Timer]
# Daily at 02:00; Persistent catches up a missed run after downtime.
OnCalendar=*-*-* 02:00:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
